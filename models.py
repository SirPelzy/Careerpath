# models.py
from flask_sqlalchemy import SQLAlchemy
from datetime import datetime # We'll likely need this

# Initialize SQLAlchemy instance
# We associate it with the app in main.py
db = SQLAlchemy()

# --- Model definitions will go here later ---
